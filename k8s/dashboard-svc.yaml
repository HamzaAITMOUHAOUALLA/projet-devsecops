apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: devsecops
  labels:
    app: dashboard
spec:
  selector:
    app: dashboard
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  type: ClusterIP
