apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: devsecops
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  type: ClusterIP
